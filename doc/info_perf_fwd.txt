Заканчивать работу будем вместе :)
 Многое уже сделано, да что там многое, почти всё. 
 Предлагаю вам довести до конца идеальную передачу. 
 Чтобы увидеть что наша идеальная передача идеальна, мы в наш контейнер попробуем разместить структуру
 struct hard {
    hard(const char *, size_t) {};
    hard(const hard &) = delete;
 };
 Добавлять в наш список будем вызовом std_alloc_lst.push_back("one", 1);
 Важная часть - это отсутствие копирования, поэтому чтобы не пропустить, в структуре он у нас будет удален.
 Хочется понять, какой у вас был план по работе с аллокатором в копирующем/перемещающем конструкторах.

 Пока выглядит так, будто в копирующем конструкторе, будет создана копия аллокатора, получается, 
 что у нас есть два аллокатора которые отвечают за работу с одной и той же памятью - похоже на фиаско.
 Аналогичная ситуация с перемещающим.
